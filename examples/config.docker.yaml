# Ops config for container deployments, passed via --config flag.

exporters:
  stdout:
    enabled: true
    format: json

server:
  listen: ":8080"   # bind all interfaces — required inside containers
